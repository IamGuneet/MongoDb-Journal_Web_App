<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <!-- CSS only -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
</head>
<body>
    <div class="container">
        <h1 class="mb-4">Journal</h1>
        <a href="/articles/new" class="btn btn-success">Add</a>
     
        <!-- //getting the articles array from the route and ittrating through each obejct in it -->
        <% articles.forEach(article =>  { %>

            <div class="card mt-4">

                <div class="card-body">
              <h4 class="card-title">  <%= article.title %> </h4>
                    <div class="card-subtitle mb-3">
                <%= article.createdAt.toLocaleDateString() %> 
                    </div>
                    <div class="card-text mb-1">
                <%= article.description %> 
                    </div>
                    <div class="mt-2">
                        <a href="/articles/<%= article.id %>" class="btn btn-primary">Read More..</a>
                        <a href="/articles/edit/<%= article.id %>" class="btn btn-info">Edit</a>
                        <!-- //using method-override to change form action to delete -->
                        <form action="/articles/<%= article.id %>?_method=DELETE" method="post" class="mt-2">
                            <button type="submit" class="btn btn-danger">Delete</button>
                        </form>
                    </div>
                   
                </div>
            </div>
       <% }) %>




    </div>
</body>
</html>